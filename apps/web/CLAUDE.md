# Frontend (`apps/web`)

Standards for this workspace live in [`docs/standards/frontend.md`](../../docs/standards/frontend.md) — shadcn/Radix controls, Tailwind tokens, forms, axios + react-query, sonner, `AiShine`, `/__dev` tooling.

Also relevant here: [`testing.md`](../../docs/standards/testing.md), [`conventions.md`](../../docs/standards/conventions.md), [`deployment.md`](../../docs/standards/deployment.md) (`VITE_API_URL` and the CSP are build-time).

**Invoke the `coding-standards` skill before implementing or reviewing.**
